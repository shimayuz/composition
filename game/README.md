# Falling Blocks Game

This directory contains a small falling block game implemented in Python using Pygame.

## How to Play

1. Install dependencies:
   ```bash
   pip install pygame
   ```
2. Run the game:
   ```bash
   python falling_blocks.py
   ```

Use the arrow keys to move and rotate the pieces. The goal is to clear as many lines as possible before the board fills up.
